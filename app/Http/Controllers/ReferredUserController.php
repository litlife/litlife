<?php

namespace App\Http\Controllers;

use App\ReferredUser;
use Illuminate\Http\Request;
use Illuminate\Http\Response;

class ReferredUserController extends Controller
{
	/**
	 * Display a listing of the resource.
	 *
	 * @return Response
	 */
	public function index()
	{
		//
	}

	/**
	 * Show the form for creating a new resource.
	 *
	 * @return Response
	 */
	public function create()
	{
		//
	}

	/**
	 * Store a newly created resource in storage.
	 *
	 * @param Request $request
	 * @return Response
	 */
	public function store(Request $request)
	{
		//
	}

	/**
	 * Display the specified resource.
	 *
	 * @param ReferredUser $referredUser
	 * @return Response
	 */
	public function show(ReferredUser $referredUser)
	{
		//
	}

	/**
	 * Show the form for editing the specified resource.
	 *
	 * @param ReferredUser $referredUser
	 * @return Response
	 */
	public function edit(ReferredUser $referredUser)
	{
		//
	}

	/**
	 * Update the specified resource in storage.
	 *
	 * @param Request $request
	 * @param ReferredUser $referredUser
	 * @return Response
	 */
	public function update(Request $request, ReferredUser $referredUser)
	{
		//
	}

	/**
	 * Remove the specified resource from storage.
	 *
	 * @param ReferredUser $referredUser
	 * @return Response
	 */
	public function destroy(ReferredUser $referredUser)
	{
		//
	}
}
